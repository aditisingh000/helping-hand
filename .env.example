## Docker Compose (local development)
## Copy to .env at repo root if you want to override defaults used by docker-compose.yml

# Postgres
POSTGRES_USER=helpinghand
POSTGRES_PASSWORD=helpinghand
POSTGRES_DB=helpinghand_dev
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379
